# Lab_Assignments_WEBDEV-KRMU
A simple JavaScript quiz game that runs in the browser using prompt() and alert(). It loops through predefined questions, checks answers by normalizing user input, gives instant feedback, and displays the final score. It demonstrates arrays, functions, loops, and conditionals in JavaScript.
